# Local Profile: allow local smoke runs to evolve the development schema.
spring.jpa.hibernate.ddl-auto=update

# Non-production fallback secret for local/dev/loadtest only.
jwt.secret=${env.JWT_SECRET:default-dev-secret-key-minimum-32-chars}
